package org.parkinglot;

public class Notification {
    public static void sendNotification(String message) {
        // Simulating sending a notification (could be email, SMS, etc.)
        System.out.println("Notification: " + message);
    }
}
